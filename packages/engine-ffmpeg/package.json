{
  "name": "@reelforge/engine-ffmpeg",
  "version": "0.0.0",
  "description": "Reelforge fast-path backend — pure ffmpeg filter_complex rendering for IRs that don't need Chrome",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src", "README.md"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@reelforge/ir": "workspace:*",
    "@reelforge/mux": "workspace:*"
  }
}
